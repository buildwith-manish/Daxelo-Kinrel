org.gradle.jvmargs=-Xmx1G -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=64m
android.useAndroidX=true
# REMOVED: android.newDsl=false — AGP 9+ requires new DSL.
# Keeping old DSL causes "Unresolved reference 'compileSdkVersion'" errors
# in plugin build.gradle.kts files (e.g. shared_preferences_android).
# Let AGP use its default (new DSL) which is compatible with Flutter 3.44.
# android.newDsl=false
android.builtInKotlin=false
org.gradle.parallel=false
org.gradle.caching=false
org.gradle.configuration-cache=false
org.gradle.workers.max=2
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
