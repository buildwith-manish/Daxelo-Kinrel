# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
# Explicit: never commit the web build output — it's rebuilt by CI
# with the correct --base-href for each deployment target.
Daxelo-Kinrel-App/build/
flutter_*.png
.metadata

# Environment & Secrets
.env
.env.local
.env.*.local

# Firebase — config files are SAFE to commit
# API keys are restricted to com.daxelo.kinrel package/bundle by Firebase
# They must be in the repo for CI/CD builds (Codemagic, GitHub Actions)
# DO NOT gitignore these files!

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Generated
*.g.dart
*.freezed.dart
*.mocks.dart

# Flutter web build
/web/build/

# iOS
ios/Pods/
ios/.symlinks/
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh

# Android
android/.gradle/
android/app/debug
android/app/profile
android/app/release
android/local.properties
android/key.properties
*.jks
*.keystore

# Coverage
coverage/
lcov.info

# Workspace artifacts
agent-ctx/
skills/
worklog.md
tool-results/
download/

# Kinrel Cameo assets are copied from /kinrel-cameo/ at repo root into
# assets/kinrel-cameo/ by scripts/setup_cameo_assets.sh and by CI.
# The canonical source is /kinrel-cameo/ — never edit the copy directly.
assets/kinrel-cameo/
