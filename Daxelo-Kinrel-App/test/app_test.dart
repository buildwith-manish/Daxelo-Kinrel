// Minimal smoke test to verify the test runner works.
// Add real tests here as the project grows.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test — app compiles and test runner works', () {
    expect(1 + 1, equals(2));
  });
}
