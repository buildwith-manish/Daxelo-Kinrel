services:
  - type: web
    name: daxelo-kinrel-server
    runtime: docker
    plan: free
    branch: main
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_PREFIX
        value: api
