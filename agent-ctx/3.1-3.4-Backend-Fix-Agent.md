# Task 3.1-3.4 — Backend Fix Agent Work Record

## Task: RolesGuard, SSRF Protection, InviterId Spoofing Fix, Support DTO Validation

### Completed Fixes

**3.1 — RolesGuard + @Roles('admin') on admin/support controllers**
- Created `common/guards/roles.guard.ts` — Reflector-based guard that reads @Roles metadata, returns true when no decorator, checks user.role
- Applied `@UseGuards(JwtAuthGuard, RolesGuard)` + `@Roles('admin')` at class level on AdminController
- Applied `@UseGuards(RolesGuard)` + `@Roles('admin')` on support `listTickets` endpoint; `listMyTickets` has no role restriction
- Registered RolesGuard as 4th global APP_GUARD in app.module.ts (after ThrottlerGuard → JwtAuthGuard → TwoFactorGuard)

**3.2 — SSRF protection for webhook URLs**
- Created `common/utils/ssrf-protection.util.ts` with `validateWebhookUrl()` — DNS resolution + private IP blocklist
- Replaced simple `startsWith('https://')` check in DeveloperService.createWebhook() with `validateWebhookUrl()` call

**3.3 — Fix inviterId spoofing + DTO validation for invitations**
- Created `modules/invitations/dto/create-invitation.dto.ts` with full class-validator decorators
- Fixed InvitationsController.create() — always uses `userId` from @CurrentUser, removed `body.inviterId`

**3.4 — DTO validation for support controller**
- Created `modules/support/dto/create-ticket.dto.ts` (subject, description, category, subcategory, severity, attachments, etc.)
- Created `modules/support/dto/add-message.dto.ts` (content with max length)
- Replaced inline body types in support.controller.ts with DTO imports

### Files Created (5)
- `server/src/common/guards/roles.guard.ts`
- `server/src/common/utils/ssrf-protection.util.ts`
- `server/src/modules/invitations/dto/create-invitation.dto.ts`
- `server/src/modules/support/dto/create-ticket.dto.ts`
- `server/src/modules/support/dto/add-message.dto.ts`

### Files Modified (5)
- `server/src/modules/admin/admin.controller.ts`
- `server/src/modules/support/support.controller.ts`
- `server/src/app.module.ts`
- `server/src/modules/developer/developer.service.ts`
- `server/src/modules/invitations/invitations.controller.ts`

### Verification
- TypeScript compilation: **0 errors** (`npx tsc --noEmit` passes cleanly)
- No new packages installed
