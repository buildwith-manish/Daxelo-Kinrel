---
Task ID: 1.8-1.12
Agent: Backend Fix Agent
Task: Fixes 1.8 through 1.12 — DTO validation, Prisma indexes, env validation, health check Redis, pagination sort injection

Work Log:

## Fix 1.8: Add DTO validation to inline and partially-validated DTOs

### 1.8a: Created notification DTOs
- Created `/server/src/modules/notifications/dto/` directory
- Created `register-fcm-token.dto.ts` with @IsString, @IsNotEmpty, @MaxLength(512) on token; @IsIn(['ios','android','web']) on deviceType
- Created `remove-fcm-token.dto.ts` with @IsString, @IsNotEmpty, @MaxLength(512) on token
- Created `mark-as-read.dto.ts` with @IsArray, @IsString({each:true}), @ArrayMaxSize(100) on notificationIds
- Created `update-preference.dto.ts` with full validation: @MaxLength(100) on eventType, @IsBoolean on channel flags, @MaxLength(5) on quietHours, @IsInt/@Min/@Max on maxPerDay, @IsIn on digestMode

### 1.8b: Updated notification controllers
- `notifications.controller.ts`: Removed inline RegisterFcmTokenDto and RemoveFcmTokenDto classes; imported from dto/ files
- `notifications-v2.controller.ts`: Removed inline MarkAsReadDto and UpdatePreferenceDto classes; imported from dto/ files

### 1.8c: Added validation decorators to existing DTOs
- `create-family.dto.ts`: Added @MaxLength(100) to name, @MaxLength(500) to description, @MaxLength(30) to primaryLanguage, @MaxLength(50) to gotra, @MaxLength(100) to originVillage
- `update-family.dto.ts`: Added @MaxLength(30) to username, @IsUrl() to avatarUrl, @MaxLength(100) to region
- `create-relationship.dto.ts`: Changed @IsString to @IsUUID() on fromPersonId and toPersonId
- `create-story.dto.ts`: Added @MaxLength(500) to caption, @IsUrl() + @MaxLength(2048) to mediaUrl, @MaxLength(100) to bgGradient

## Fix 1.9: Add missing database indexes to Prisma schema

Added these @@index declarations:
- Invitation model: `@@index([recipientEmail])` and `@@index([status])`
- FamilyInvite model: `@@index([familyId])` and `@@index([status])`
- RefreshToken model: `@@index([userId, revokedAt])` — already existed, skipped
- FcmToken model: `@@index([userId])` — already existed, skipped
- UsernameChangeLog model: `@@index([userId])`
- User model: `@@index([phone])`
- Family model: `@@index([createdBy])`
- FamilyMember model: `@@index([familyId, role])`
- Story model: `@@index([userId, expiresAt])`

Ran `npx prisma format` successfully.

## Fix 1.10: Fix environment validation

- Added new required vars: CLOUDINARY_CLOUD_NAME, CLOUDINARY_API_KEY, CLOUDINARY_API_SECRET, DEEPSEEK_API_KEY, SUPABASE_JWT_SECRET
- Removed GOOGLE_AI_API_KEY from REQUIRED_VARS (replaced with DEEPSEEK_API_KEY)
- Added RECOMMENDED_VARS array: REDIS_URL, SMTP_HOST, SMTP_USER, SMTP_PASS, RAZORPAY_KEY_ID, RAZORPAY_KEY_SECRET
- In production (NODE_ENV=production), STRICT_CONFIG defaults to true unless explicitly set to 'false'
- Added separate check for RECOMMENDED_VARS that logs warnings but never crashes
- Removed placeholder DATABASE_URL fallback

## Fix 1.11: Fix health check — add Redis check

- Injected ConfigService into HealthController
- Created a lightweight ioredis client (lazyConnect, 3s timeout, no retries)
- Added Redis PING check in the health endpoint
- Returns `redis: 'ok' | 'error'` in the response
- Updated HealthModule to import PrismaModule for DI
- Health status is now 'ok' only when both db and redis are 'ok'

## Fix 1.12: Fix pagination sort field injection

- Added SAFE_SORT_FIELDS whitelist: createdAt, updatedAt, name, email, id, startDate, expiresAt, joinedAt
- Added SafeSortField type
- Updated paginationToPrisma() to accept optional `allowedSortFields` parameter
- Sort field is now sanitized against whitelist — unknown fields fall back to 'createdAt'
- Fixed TypeScript type issue with readonly string[] for includes() check

## Verification
- TypeScript compilation: `npx tsc --noEmit` passes with 0 errors
