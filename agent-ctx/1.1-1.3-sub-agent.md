# Task 1.1-1.3: Security Fixes

## Agent: Sub Agent

## Summary
Completed 3 security fixes across the Daxelo-Kinrel Flutter app.

## Changes

### Fix 1.1: kAuthDisabled → --dart-define controlled
- **File**: `lib/core/config/auth_config.dart`
- Changed `const bool kAuthDisabled = true` → `bool.fromEnvironment('AUTH_DISABLED', defaultValue: false)`
- Production-safe: auth is now ENABLED by default; developers must explicitly pass `--dart-define=AUTH_DISABLED=true`

### Fix 1.2: Fix 13 route parameter force-unwraps
- **File**: `lib/core/routing/app_router.dart`
- Added `_param(GoRouterState state, String key)` helper after `_instantPage`
- Replaced all 13 `state.pathParameters['id']!` / `['code']!` / `['key']!` with `_param(state, 'id')` / `_param(state, 'code')` / `_param(state, 'key')`
- Missing params now return empty string instead of crashing the app

### Fix 1.3: Remove debug password from source code
- **File**: `lib/core/services/supabase_service.dart`
- Added `const String _debugPassword = String.fromEnvironment('DEBUG_PASSWORD', defaultValue: '')`
- Replaced both `'Debug@123456'` occurrences with `_debugPassword`
- Added early log when `_debugPassword.isEmpty`
- Wrapped email sign-in and sign-up blocks in `if (_debugPassword.isNotEmpty) { ... }`

## Verification
- Grep confirms zero remaining `pathParameters[...]!` force-unwraps in app_router.dart
- Grep confirms zero remaining `Debug@123456` in supabase_service.dart
- No new imports added
