---
Task ID: 3.6-3.9
Agent: Fix Agent
Task: Phone validation, touch targets, 404 route, Logger, Semantics

Work Log:
- 3.6a: Added `validator: (value) => phoneValidator(value),` to the phone TextFormField in sign_up_screen.dart. The form_validators.dart import already existed (line 16), and phoneValidator was already defined there (7-15 digit validation with character checks).
- 3.6b: Removed `minimumSize: Size.zero` and `tapTargetSize: MaterialTapTargetSize.shrinkWrap` from 3 TextButton style declarations:
  - sign_in_screen.dart: "Forgot Password?" button and "Sign Up" link
  - sign_up_screen.dart: "Sign In" link
  This ensures minimum 48px touch targets for accessibility compliance.
- 3.7a: Added `errorBuilder` parameter to GoRouter in app_router.dart — renders a 404 page with "Page not found" message, the unmatched path, and a "Go Home" button that navigates to /home. Uses Outfit font family consistent with app branding.
- 3.7b: Replaced 4 duplicate routes with redirects:
  - `/family/:id/add-member` → redirects to `/family/:id/add-person`
  - `/legal/terms` → redirects to `/terms`
  - `/legal/privacy` → redirects to `/privacy`
  - `/profile/premium` → redirects to `/premium`
- 3.8: Replaced all `console.log`/`console.warn` calls in kinrel.gateway.ts with NestJS Logger:
  - Added `import { Logger } from '@nestjs/common';`
  - Added `private readonly logger = new Logger(KinrelGateway.name);` to class
  - Replaced 6 console calls: 3 `console.warn` → `this.logger.warn`, 2 `console.log` → `this.logger.log`, 1 combined `console.warn` with two args → single `this.logger.warn` call
  - Removed `[WS]` prefix from all messages since Logger already includes the class name
- 3.9a: Wrapped Google Sign-In button in sign_in_screen.dart with `Semantics(button: true, label: 'Sign in with Google')`. Email and password TextFormFields already have hintText which provides accessibility labels automatically.
- 3.9b: Wrapped family card DKCard in family_list_screen.dart with `Semantics(button: true, label: 'Family: ${family.name}', hint: 'Tap to view family details')`.

Stage Summary:
- 5 files modified: sign_up_screen.dart, sign_in_screen.dart, app_router.dart, kinrel.gateway.ts, family_list_screen.dart
- TypeScript compilation: 0 errors (npx tsc --noEmit passes cleanly)
- Dart CLI unavailable in sandbox — verify Flutter compilation locally
- All 4 fixes (3.6-3.9) complete
